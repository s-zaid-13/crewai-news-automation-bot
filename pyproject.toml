[project]
name = "signal-desk-dashboard"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "gspread>=6.1.4",
    "google-auth>=2.35.0",
]

[tool.vercel]
entrypoint = "api.news:handler"